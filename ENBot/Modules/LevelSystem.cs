using Discord.Commands;
using System;
using System.Collections.Generic;
using System.Text;
using System.Threading.Tasks;

namespace ENBot.Modules
{
    public class LevelSystem
    {
        /*public double GetLevelEquation(int level)
        {
            double xp = Math.Floor(Math.Round(50 * Math.Pow(level + 1, 2)));
            return xp;
        }

        public bool CanLevelUp(int level, int currentXp)
        {
            double needXp = GetLevelEquation(level);
            if(currentXp >= needXp)
            {
                return true;
            }
            return false;
        }

        public async Task LevelUp(SocketCommandContext context)
        {

        }*/
    }
}
